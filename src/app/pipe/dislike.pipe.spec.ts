import {DislikePipe} from './dislike.pipe';

describe('DislikePipe', () => {
    it('create an instance', () => {
        const pipe = new DislikePipe();
        expect(pipe).toBeTruthy();
    });
});
