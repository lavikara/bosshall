import { URLStrREplacerPipe } from './urlstr-replacer.pipe';

describe('URLStrREplacerPipe', () => {
  it('create an instance', () => {
    const pipe = new URLStrREplacerPipe();
    expect(pipe).toBeTruthy();
  });
});
