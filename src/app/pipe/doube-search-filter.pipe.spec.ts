import { DoubleSearchFilterPipe } from './doube-search-filter.pipe';

describe('DoubeSearchFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new DoubleSearchFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
