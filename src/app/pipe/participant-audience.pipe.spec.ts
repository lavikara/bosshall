import { ParticipantAudiencePipe } from './participant-audience.pipe';

describe('ParticipantAudiencePipe', () => {
  it('create an instance', () => {
    const pipe = new ParticipantAudiencePipe();
    expect(pipe).toBeTruthy();
  });
});
