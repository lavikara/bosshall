import {AudiencePipe} from './audience.pipe';

describe('AudiencePipe', () => {
    it('create an instance', () => {
        const pipe = new AudiencePipe();
        expect(pipe).toBeTruthy();
    });
});
